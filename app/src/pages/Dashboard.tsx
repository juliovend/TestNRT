import { Card, CardContent, Typography } from '@mui/material';

export default function Dashboard() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h5">Dashboard</Typography>
        <Typography>Bienvenue dans l'application de gestion des TNR.</Typography>
      </CardContent>
    </Card>
  );
}
